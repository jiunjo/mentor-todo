import Button from "../components/Button.jsx";

export default function MainPage() {
  return (
    <div>
      <Button mode="create">Todo 생성</Button>
      <Button mode="update">Todo 수정</Button>
    </div>
  );
}
